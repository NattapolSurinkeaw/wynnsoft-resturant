import { User } from "../models/users";
import { UsersPermission } from "../models/usersPermission";

export class WebSettingController {
  // OnsettingUser = async (req: any, res: any) => {
  //   const userAll = await User.findAll();
  //   const permisstion = await UsersPermission.findAll();
  //   return res.status(200).json({
  //     status: true,
  //     message: "ok",
  //     description: "Get setting success.",
  //     user: userAll,
  //     permisstion: permisstion
  //   });
  // }
}